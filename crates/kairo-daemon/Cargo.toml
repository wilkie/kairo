[package]
name = "kairo-daemon"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
axum = "0.7"
hyper = { version = "1", features = ["server", "http1"] }
hyper-util = { version = "0.1", features = ["server", "server-auto", "server-graceful", "http1", "tokio"] }
kairo-core = { path = "../kairo-core" }
kairo-daemon-client = { path = "../kairo-daemon-client" }
kairo-identity = { path = "../kairo-identity" }
kairo-statement = { path = "../kairo-statement" }
kairo-store = { path = "../kairo-store" }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "fs", "signal", "sync", "io-util", "time"] }
tokio-util = { version = "0.7", features = ["io"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
utoipa = { version = "5", features = ["axum_extras"] }

[dev-dependencies]
hyper = { version = "1", features = ["client", "http1"] }
hyper-util = { version = "0.1", features = ["client", "client-legacy", "http1", "tokio"] }
http-body-util = "0.1"
kairo-test-support = { path = "../kairo-test-support" }
tempfile = "3"

[lib]
name = "kairo_daemon"
path = "src/lib.rs"

[[bin]]
name = "kairo-daemon"
path = "src/main.rs"

[lints]
workspace = true
