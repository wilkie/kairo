[package]
name = "kairo-statement"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kairo-core = { path = "../kairo-core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lib]
name = "kairo_statement"
path = "src/lib.rs"

[lints]
workspace = true
