// Slice 5 stub. Slice 9 fills in the validation-viewer package
// (issue list, statement-graph view, authority-chain view,
// `valid` / `invalid` / `conflicted` / `indeterminate` /
// `unverified` badges) — `WEB_CLIENT.md` §4.5 and §10.

export { ValidationBadge, type ValidationBadgeProps } from './ValidationBadge';
