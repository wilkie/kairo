// Slice 5 stub. Slice 10 ships the registry + viewer set
// (text, JSON, binary placeholder + download) per
// `WEB_CLIENT.md` §12 and `PHASE_2_WEB_CLIENT.md` slice 10.
// Image / audio / video / markdown / CSV viewers are deferred
// post-v1.

export { BlobPreview, type BlobPreviewProps } from './BlobPreview';
