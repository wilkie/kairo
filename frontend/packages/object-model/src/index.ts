// Slice 5 stub. Slice 6 lands DTO type re-exports, identifier
// formatters (truncated display, copy helpers), display enums,
// and type guards for API payload shape — per `WEB_CLIENT.md`
// §4.4. Anything semantic stays on the daemon side; this
// package is presentation helpers only.

export const PACKAGE_NAME = '@kairo/object-model';
