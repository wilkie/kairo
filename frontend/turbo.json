{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "generate:api"],
      "inputs": [
        "src/**",
        "index.html",
        "tsconfig.json",
        "package.json",
        "vite.config.ts",
        "vite.config.js"
      ],
      "outputs": ["dist/**", "build/**", ".tsbuildinfo"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "interactive": true
    },
    "typecheck": {
      "dependsOn": ["^build", "generate:api"],
      "inputs": ["src/**", "tsconfig.json", "package.json"]
    },
    "lint": {
      "dependsOn": ["generate:api"],
      "inputs": [
        "src/**",
        "package.json",
        "tsconfig.json",
        "../../eslint.config.js"
      ]
    },
    "test": {
      "dependsOn": ["^build", "generate:api"],
      "inputs": ["src/**", "tests/**", "package.json"]
    },
    "generate:api": {
      "inputs": ["../../openapi/kairo-daemon.json", "package.json"],
      "outputs": ["src/generated/**"]
    },
    "storybook:build": {
      "inputs": ["src/**", ".storybook/**", "package.json"],
      "outputs": ["storybook-static/**"]
    },
    "storybook": {
      "cache": false,
      "persistent": true,
      "interactive": true
    }
  }
}
