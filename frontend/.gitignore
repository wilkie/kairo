node_modules
.turbo
.pnpm-store
dist
build
*.tsbuildinfo
.cache
storybook-static

# Playwright output (per-test artifacts + HTML report)
test-results
playwright-report
playwright/.cache

# Local environment overrides (never commit)
.env
.env.*
!.env.example
